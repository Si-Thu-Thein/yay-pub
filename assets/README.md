# Repo assets

Marketing / documentation assets referenced from `README.md` (e.g. app icon screenshots). Not bundled into the app — for `Yay.app` icon assets see `Yay/Assets.xcassets/` and `AppIcon.icon/`.
